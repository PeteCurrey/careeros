# Supabase (Public & Shared)
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-Only Credentials (NEVER expose to client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Secret used to HMAC-sign the end-user session cookie. Must be at least 32
# characters. If unset, SUPABASE_SERVICE_ROLE_KEY is used instead; setting a
# dedicated value is preferred so sessions can be invalidated (by rotating this)
# without rotating database credentials.
# CAREEROS_SESSION_SECRET=

# App Environment
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_ENV=development

# Dedicated Admin Authentication Security Settings (Server-Only)
ADMIN_SESSION_IDLE_MINUTES=30
ADMIN_SESSION_MAX_HOURS=8
ADMIN_STEP_UP_MINUTES=15
ADMIN_LOGIN_MAX_ATTEMPTS=5
ADMIN_LOGIN_WINDOW_MINUTES=15

# Auth Providers (optional OAuth)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# MICROSOFT_CLIENT_ID=
# MICROSOFT_CLIENT_SECRET=
